
public class README {
/*全都在github里
 * sample-project：stm的项目,springboot+spring data jpa+spring security+lombok+RabbitMQ+Kafka+ELK+ES
 * seckill：redis+mq+mybatis（xml）比较正式的秒杀例子(mysql)+mycat(又走普通mysql又走mycat不行，搞了个新的项目mycat）+HttpClient测试（在util包里）+静态文件（暂不太懂）
 * newspringbootseckill:另一套比较标准的秒杀，慕课的，非常类似上面的可以对照着看
 * springboot-seckill：比较简单的秒杀(xu_xi_rd2)SeckillServiceImpl有redis测试
 * demo:中间件那本书的kafka一章的小应用
 * chapter-4+4个dubbo的：以chapter-4（是否可以删）为首的4个module子项目，微服务这本书dubbo的chapter-4里项目和根目录的是相通的，运行的话去根目录下面的项目运行
 *                       写代码也在子项目写。凭印象：
 *                       api注重提供接口（方法），client是直接在applications里调用，gateway是通过路径（controller）调用，server implements api提供服务
 * chapter-13-3+3个跟了SNAPSHOT的：dubbo+Oauth2，主要就是gateway那个子模块项目，但两个都要启动。（好像导入maven项目时，所有的pom都要导入。。。。）
 * mycat:测试mycat+spring的小项目（注解式mybatis）
 * bootshardingstudy：用shardingJDBC进行分库分表
 * springboot-jjwt:简单的springboot+JWT,详见https://www.jianshu.com/p/e88d3f8151db
 * generatorutil:mybatis自动生成(采坑无数)+简单操作 
 * redisCluster:简单的redisCluster+spring例子，没测试过，估计是可以的
 * x-springboot:一个可用的简单后台系统,springboot+redis+mybatisplus+vue
 * SMRRS(MySpringBoot):一个简单的商铺系统,SpringBoot+Mybatis+Druid+Pagehelper+Redis+Shiro+RabbitMQ+Freemarker+poi
 * quick-cli：以该项目为首的一系列quick dubbo项目，作者介绍是：基于Dubbo微服务项目快速搭建脚手架，提供基础功能，方便企业快速搭建项目。
 *            主要技术Dubbo/SpringBoot/Jpa/Redis/MySQL/RabbitMq/Shiro/Zookeeper 
 * SMRR(spring-boot-06-data-mybatis):非常非常简单基本的SpringBoot集成MyBatis、redis、RabbitMQ、Elasticsearch、定时任务
 * springbootES(spring5):用了ES的springboot(还有一些常规的中间件)
 */
}
